window.ms_wp_registered_users = ( function( $, window, document ) {

	var app = {};

	app.init = function() {

	};

	app.cache = function() {

	};

	$( document ).ready( app.init );

	return app;

})( jQuery, window, document );